<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dong.catdogMapper">

  	<!-- 로그인 -->
 	<select id="login" parameterType="java.util.Map" resultType="java.util.Map">
 		select user_id,name,user_auth from member
 		where user_id=#{user_id} and password=#{password}
 	</select>

	<!-- 카테고리 01 리스트 -->
	<select id="list" parameterType="java.lang.Integer" resultType="ProductDTO">
		select *
		from product
		where
		product_category=#{product_category}
		order by product_regdate desc
	</select>
	
	<!-- 리스트에서 제품 선택 -->
	<select id="product_detail" parameterType="java.lang.Integer"
		resultType="BoardDTO">
		select *
		from product
		where product_code = #{product_code}
	</select>

	<!-- 찜 추가 -->
	<insert id="addWish" parameterType="WishDTO">
		insert into wish
		values
		(#{user_id}, #{product_code})
	</insert>

	<!-- 찜 제거 -->
	<delete id="deleteWish" parameterType="WishDTO">
		delete from wish
		where
		user_id=#{user_id} and product_code = #{product_code}
	</delete>


</mapper>